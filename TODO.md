# TODO — Semaine 4 (Orchestrator)

- [x] Modifier `orchestrator.py` : ajouter une mémoire locale anti-doublon (`tickets_deja_traites = set()`) pour éviter le traitement multiple d’un même ticket pendant une session.
- [x] Mettre à jour `README.md` : ajouter la section `## Semaine 4 — Orchestrator` (fonctionnalités, variable d’environnement, exécution, workflow).
- [ ] Vérifier `git status` (aucun ajout de `.env` / `orchestrator.log`).
- [ ] Commit : `feat: add orchestrator anti-duplicate memory` (message final ajusté si besoin) et `push`.


